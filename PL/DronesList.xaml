<Window x:Class="PL.DronesList" 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PL"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DronesList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="400*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"  Name="UpGrid" HorizontalAlignment="Stretch" Height="auto" VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions >

            <TextBox Name="TBD" Grid.Column="0"></TextBox>
            <ComboBox Grid.Column="1" x:Name="DroneWeights" HorizontalAlignment= "Stretch" VerticalAlignment="Stretch" />
            <ComboBox Grid.Column="2" x:Name="DroneStatuses" HorizontalAlignment= "Stretch" VerticalAlignment="Stretch"/>
            <ComboBox Grid.Column="3" Name="StatusSelector4" HorizontalAlignment= "Stretch" VerticalAlignment="Stretch"/>

        </Grid>
        <ListView  x:Name="DronesListView" ItemsSource="{Binding}" Grid.Row="1">
            <ListView.View>
                
            </ListView.View>
        </ListView>

    </Grid>
</Window>
